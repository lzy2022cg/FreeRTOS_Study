# FreeRTOS 时间片调度与系统时钟节拍

## 问题

> 教材 5.4.2「时间片调度」里说：FreeRTOS 的时间片轮转是固定时间片，每个任务分到的时间片长度完全相同，与任务复杂度无关。时间片调度主要针对优先级相同的任务，当多个任务优先级相同时，任务调度器会在每一次系统时钟节拍到来时切换任务，也就是 CPU 轮流运行优先级相同的任务，每个任务运行的时间就是一个系统时钟节拍。
>
> 这里说的「系统时钟节拍」说的就是时间片到了吧？切换任务是 Tick 中断吗？

## 回答

你的理解基本对，但有两个地方需要更精确一点，下面把整条链路理清楚。

### 1. 系统时钟节拍 ≠ 时间片，而是「时间片 = 1 个节拍」

- **系统时钟节拍（Tick）**：是 FreeRTOS 的心跳，一个**周期性的定时中断**。周期由 `configTICK_RATE_HZ` 决定，比如 1000Hz 就是每 1ms 来一次。在 STM32 上通常用 **SysTick 定时器**产生。
- **时间片（Time Slice）**：FreeRTOS 里**默认一个时间片就等于一个节拍周期**。

所以不是「节拍 = 时间片到了」，准确说是：**FreeRTOS 把时间片的长度直接定义成 1 个节拍**。每来一个节拍，同优先级任务的这一片就用完了，于是轮换。这也就是教材里说的「固定时间片、每个任务长度完全相同」——因为大家都是 1 个 tick。

### 2. 切换不是在 Tick 中断里直接完成的（重要细节）

「切换任务是 Tick 中断」方向对，但实际分两步：

1. **SysTick 中断** → 调用 `xPortSysTickHandler()` → 里面调 `xTaskIncrementTick()`。
   - 它负责累加系统时钟、检查延时到期的任务、判断同优先级是否需要轮转。
   - 如果需要切换，它只是**挂起一个 PendSV 异常**（置位），并不真正切栈。
2. **PendSV 中断**（最低优先级）→ `xPortPendSVHandler()` → **真正的上下文切换在这里做**（保存旧任务寄存器、选新任务、恢复新任务寄存器）。

这样设计是为了让真正耗时的「保存/恢复现场」放到优先级最低的 PendSV 里，不会阻塞其他更重要的中断。

### 3. 还有一个前提：不是每个节拍都一定切换

时间片轮转只在**同一优先级有 ≥2 个就绪任务**时才发生。如果某优先级下只有一个任务在跑，节拍照样来，但 `xTaskIncrementTick()` 判断没有同级竞争者，就**不会触发 PendSV，任务继续跑**。

另外要生效还需要两个配置宏都打开（默认都是开的）：

- `configUSE_PREEMPTION = 1`
- `configUSE_TIME_SLICING = 1`

---

**一句话总结**：节拍是时钟源，时间片默认就等于 1 个节拍；同级任务竞争时，节拍到了会在 SysTick 中触发 PendSV，**真正的切换发生在 PendSV 里**。
