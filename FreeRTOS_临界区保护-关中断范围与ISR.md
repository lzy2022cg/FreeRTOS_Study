# FreeRTOS 临界区保护：到底关了哪些中断 & 「ISR 也靠中断」

> 背景：学习「临界区保护及调度器的挂起和恢复」时的两个问答。平台为 Cortex-M（STM32F407）。

---

## 问题一：临界区是关闭了所有的中断吗？

**不是**——在 Cortex-M（STM32F407）上，`taskENTER_CRITICAL()` 进入临界区**并没有关掉所有中断**，只关掉了「一部分」。常见说法「临界区直接屏蔽了中断」是个**简化**，精确真相和 `configMAX_SYSCALL_INTERRUPT_PRIORITY` 直接挂钩。

### 它到底关了哪些中断

`taskENTER_CRITICAL()` 在 Cortex-M 上做的事是：

> 把 **`BASEPRI` 寄存器设成 `configMAX_SYSCALL_INTERRUPT_PRIORITY`**（F407 上一般是 5）。

`BASEPRI` 的效果是**只屏蔽优先级数值 ≥ 阈值的中断**：

```
中断优先级数值:   0   1   2   3   4 │ 5   6  ...  15
                  ├─────────────────┤ ├──────────────┤
临界区里:          ✅ 照常运行!        │   ❌ 被屏蔽
                 (高于阈值,内核管不着)  │ (受管中断,被挡住)
                                     ↑
                  taskENTER_CRITICAL 把 BASEPRI 抬到这里(=5)
```

- **数值 ≥ 5 的中断（受 FreeRTOS 管的）→ 被关掉**，临界区里它们进不来；
- **数值 0~4 的中断（高于阈值的“零延迟中断”）→ 照样响应**，临界区根本挡不住它们。

### 为什么这么设计

如果真的「关所有中断」（`CPSID i` 全关），那些最紧急的实时中断（比如电机急停、过流保护）也会被延迟，实时性就毁了。FreeRTOS 的妥协是：

> 临界区只屏蔽「可能会调用内核 API、会动内核数据」的那批中断（受管中断），把内核数据保护好就够了；至于更高优先级的中断，反正它们本来就不准碰任何 FreeRTOS API（这就是 FromISR 那条规则的由来），不会破坏内核数据，所以放它们继续跑。

串起来：

- 临界区屏蔽受管中断 → 受管中断进不来 → 不会在内核改数据时插队 → 数据安全；
- 高优先级中断不被屏蔽 → 但它们不准调 API → 也不会破坏数据。

### 移植相关的补充

这是**移植相关**的。在 8051/AVR 这种简单 MCU 上，`taskENTER_CRITICAL()` 确实是 `cli` 直接关全部中断（它们没有 BASEPRI 这种分级屏蔽）。所以教材笼统说「关中断」在那些平台上没错，但 **Cortex-M 上是分级屏蔽**。

### 对比：临界区 vs 挂起调度器

| | 临界区 `taskENTER_CRITICAL` | 挂起调度器 `vTaskSuspendAll` |
|---|---|---|
| 关中断吗 | 关（到阈值，BASEPRI） | **完全不关中断** |
| 怎么保护 | 屏蔽受管中断，连 ISR 都挡 | 只置 `uxSchedulerSuspended`，禁止**任务切换** |
| 中断能响应吗 | 受管中断不行 | **所有中断照常响应**（但 tick 里不会触发切换） |
| 适用 | 临界区在任务和 ISR 之间共享时 | 只防任务间抢占，不想耽误中断时 |

**小结**：Cortex-M 上临界区不是关全部中断，而是用 `BASEPRI` 关掉「数值 ≥ configMAX_SYSCALL_INTERRUPT_PRIORITY」的受管中断；更高优先级的中断照跑，但它们本来就不准调 API。

---

## 问题二：「ISR 也靠中断」这句话是什么意思？

思维导图里完整的一句是：

> 「临界区直接屏蔽了中断，**系统任务调度靠中断，ISR 也靠中断**」

作者是在**解释「为什么关中断就能保护临界区」**。

### 先看：谁能打断你的临界区代码？

一段代码正在跑，能把它打断的「敌人」只有两类：

1. **别的任务把 CPU 抢走**（任务切换/调度）
2. **中断服务程序（ISR）插进来执行**

而这两类敌人，**都离不开中断**：

- **「系统任务调度靠中断」** → 任务切换是靠 **SysTick 中断**驱动的：tick 到了才进中断、才可能触发 PendSV 去切任务。没有这个中断，调度器根本动不了。
- **「ISR 也靠中断」** → 意思是：**ISR（中断服务函数）只有在它对应的中断被触发时才会运行**，中断就是 ISR 的「启动开关」。没有中断发生，ISR 就不会执行。

### 所以「ISR 也靠中断」=

> 中断服务程序本身是由中断触发才跑起来的；既然临界区已经把中断屏蔽了，中断就发不出来，对应的 ISR 自然也不会运行，也就插不进来打断你的临界区代码。

整句串起来就是作者觉得「强悍」的原因：

```
关中断  ──┬──→ SysTick 不来 → 不触发 PendSV → 任务不会被切走   (挡住敌人①)
          └──→ 中断不发生  → ISR 不执行       → ISR 插不进来    (挡住敌人②)
                              ↓
                   临界区代码一气呵成，不被任何东西打断
```

一刀（关中断）同时挡住了「任务抢占」和「ISR 插队」两条路，这就是它「强悍」之处——比单纯挂起调度器（只挡任务、不挡 ISR）更彻底。

### 精确性补丁

还是要记得：Cortex-M（F407）上「关中断」不是关全部，而是用 `BASEPRI` 关到 `configMAX_SYSCALL_INTERRUPT_PRIORITY` 阈值。所以严格说：

- 被挡住的是**受管中断**（数值 ≥ 阈值）对应的 ISR；
- 优先级更高（数值 < 阈值）的 ISR **照样能触发运行**——但它们本来就不准碰 FreeRTOS API，不会破坏内核数据，所以不影响临界区保护的目的。

**小结**：「ISR 也靠中断」是说 ISR 得靠中断触发才运行，所以一旦关了中断，ISR 就跑不起来、打断不了临界区——这是作者用来论证「关中断为啥能保护临界区」的两个依据之一（另一个是「调度也靠中断」）。
